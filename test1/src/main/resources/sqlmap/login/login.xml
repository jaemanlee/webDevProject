<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> <!-- 맵핑될 DAO 인터페이스의 Full name 을 줍니다. -->

<mapper namespace="loginDAO">

   <insert id="login"  parameterType="com.lee.test1.login.dvo.loginDVO">
      insert into user_info values(
         #{userId},
         #{userPassword},
         #{userAddr},
         #{userSex},
         #{userPhone},
         TO_DATE(SYSDATE, 'yyyy mm dd'),
         #{userBirth},
         #{mailNo}
      )
   </insert>
   
   <select id="temp_01" parameterType="com.lee.test1.login.dvo.loginDVO">
      select *
     from user_info
    
   </select>

</mapper>